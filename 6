INNER JOIN возвращает только те строки, для которых есть совпадение в обеих таблицах.Если строка в одной из таблиц не имеет соответствующей строки в другой таблице, эта строка исключается из результата.
LEFT JOIN возвращает все строки из левой таблицы и только те строки из правой таблицы, которые с ними совпадают. Если для строки из левой таблицы нет соответствующих строк в правой таблице, то в результирующей таблице значения из правой таблицы будут заполнены NULL.

Думаю LEFT JOIN потенциально может вернуть больше строк, чем INNER JOIN, поскольку он сохраняет все строки из левой таблицы, даже если для некоторых из них нет совпадений в правой таблице. В этих случаях в результат добавляются строки с NULL-значениями из правой таблицы.